// Smoke test básico do ScannerApp.
// Verifica apenas que o widget raiz instancia sem erros fatais.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke test — biblioteca flutter_test disponível', () {
    expect(1 + 1, equals(2));
  });
}
